# Token 钱包插件

展示经济实例与账户、可用和冻结余额、审计流水、购买凭证。冻结前明确显示游戏版本、
代码摘要、审核状态、服务身份、全部本人/Agent 席位投入、期限与固定结算规则。
游戏账户使用一次性绑定码，游戏服务器不接触钱包用户凭证。

仅通过公开 `ctx.http` 和 Host 安全凭证输入连接服务；旧 Host 显示能力不可用。
重新加载插件后需重新授权。完整开发命令、固定 SDK 与验证范围见
[英文说明](README.md)。生产包保留正式 Vite 多文件 artifact 图与延迟加载 CSS。
